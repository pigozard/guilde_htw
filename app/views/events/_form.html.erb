<%= form_with model: @event, local: true, class: "event-form" do |f| %>
  <% if @event.errors.any? %>
    <div class="alert-error mb-4">
      <ul class="mb-0">
        <% @event.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= f.label :title, "Titre", class: "form-label" %>
    <%= f.text_field :title, class: "form-control", placeholder: "Ex: Raid Nerub'ar" %>
  </div>

  <div class="form-group mb-3">
  <%= f.label :event_type, "Type d'événement", class: "form-label" %>
  <%= f.select :event_type, [
    ["⚔️ Raid", "raid"],
    ["🔮 Mythic+", "mythic+"],
    ["🏆 PvP", "pvp"],
    ["🎉 Social", "social"],
    ["📌 Autre", "other"]
  ], {}, class: "form-control" %>
</div>

<div class="form-group mb-3">
  <%= f.label :start_time, "Date et heure", class: "form-label" %>
  <div class="d-flex gap-2">
    <%= f.date_field :start_date, class: "form-control", value: @event.start_time&.to_date || Date.today %>
    <%= f.time_field :start_hour, class: "form-control", value: @event.start_time&.strftime("%H:%M") || "21:00", style: "max-width: 120px;" %>
  </div>
</div>
  <div class="form-group mb-3">
    <%= f.label :max_participants, "Nombre max de participants (optionnel)", class: "form-label" %>
    <%= f.number_field :max_participants, class: "form-control", placeholder: "Ex: 20" %>
  </div>

  <div class="form-group mb-4">
    <%= f.label :description, "Description (optionnel)", class: "form-label" %>
    <%= f.text_area :description, class: "form-control", rows: 3, placeholder: "Détails de l'événement..." %>
  </div>

  <div class="d-flex gap-3">
    <%= f.submit "Enregistrer", class: "btn-gold" %>
    <%= link_to "Annuler", events_path, class: "btn-outline-gold" %>
  </div>
<% end %>
